import React from "react";
import ReactDOM from "react-dom/client";
import { Demo } from "./Demo";
import { DemoObj } from "./DemoObj";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <DemoObj />
  </React.StrictMode>
);
